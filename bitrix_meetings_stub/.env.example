# Server
PORT=3000

# true = read local mock-data.json
# false = call real Bitrix webhook
USE_MOCK=true

# Bitrix (required when USE_MOCK=false)
BITRIX_WEBHOOK_URL=https://your-domain.bitrix24.kz/rest/1/your-webhook-token
BITRIX_MEETING_STAGE_ID=NEW_STAGE_ID
